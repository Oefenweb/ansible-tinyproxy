# test file for tinyproxy
---
- hosts: localhost
  connection: local
  become: true
  roles:
    - ../../
  vars:
    tinyproxy_tinyproxy_conf:
      - |
        User nobody
        Group nogroup
        Port {{ tinyproxy_port }}
