# vars file
---
tinyproxy_port: 3128
tinyproxy_allow:
  - "{{ ansible_facts['lo']['ipv4']['address'] }}"
  - "{{ ansible_facts['lo']['ipv6']['address'] }}"
  - "{{ ansible_default_ipv4['address'] }}"
